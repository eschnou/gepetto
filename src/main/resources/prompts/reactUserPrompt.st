The goal of this test suite:
{task}

The actions already executed are the following:
{history}

The test you are now running is the following. Only execute what is required to run this exact test:
{instructions}